# Core DreamerV3 dependencies.
#
# Install environment-specific suites (dm_control, crafter, atari, minigrid)
# separately — see README.md.

dreamerv3>=3.0.0
jax>=0.4.26
jaxlib>=0.4.26
numpy>=1.24
gymnasium>=0.29
tensorboard>=2.14
ruamel.yaml>=0.18
cloudpickle>=3.0
