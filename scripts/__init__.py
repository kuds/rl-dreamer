"""Top-level package for the training / evaluation / visualization scripts.

Making ``scripts`` a package lets notebooks and other callers import the
library entry points directly, e.g. ``from scripts.visualize_dreams import
DreamVisualizer``. The modules are still runnable as CLI scripts via
``python scripts/<name>.py``.
"""
